package de.sbicher.tc_clown.settings;

import java.io.File;

/**
 * Reader, to read the settings from XML-Files
 */
public class TcSettingsReader {
    /**
     * Reads the settings from the tc_settings.xml
     * @param settingsFile xml file with the settings
     * @return Settings for the application
     */
    public TcSettings readSettings(File settingsFile) {
        if (true) {
            throw new IllegalStateException("not yet implementend");
        }
        return new TcSettings();
    }
}
